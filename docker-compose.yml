services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app/data:/project/app/data
    environment:
      - SBTI_CONFIG_PATH=/project/app/config.json
    restart: unless-stopped
